## 修復

- **自動更新 404 修正**：macOS 打包檔案名稱含空格（`SkillCraft IMS-x.x.x-arm64-mac.zip`），導致 GitHub Release 下載 URL 空格被轉為 dash，electron-updater 解析不一致而出現 404 錯誤。現透過 `artifactName` 明確指定不含空格的檔名（`SkillCraft-IMS-x.x.x-arm64-mac.zip`），與 GitHub 下載 URL 完全一致。
