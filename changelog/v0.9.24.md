## 庫存異動歷史 — 日期快選預設 ＋ 緊急修復

### 改善

- **庫存異動歷史頁**：日期篩選欄新增「今天 / 本週 / 本月 / 上月 / 近90天」快選按鈕列
- 點擊預設按鈕自動填入對應日期區間，表格即時篩選
- 已選中的預設按鈕以主色高亮標示；手動修改日期後高亮自動消除
- 現有「清除日期」（X）按鈕仍可一鍵重置，兩者協同運作

與銷售單、採購單頁面的日期快選體驗完全一致。

### 緊急修復

- **AI 品質分析頁無限載入**：`db/index.ts` 缺少 migration 022–028 的 import 與陣列項目，導致資料庫欄位（`input_tokens`、`faithfulness_score`）和資料表（`ai_entity_stats`）從未建立；補齊後品質分析頁恢復正常
